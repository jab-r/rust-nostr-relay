//! Group registry services for MLS messaging

/// Group registry service functions
pub struct GroupRegistry;

impl GroupRegistry {
    /// Create a new group registry
    pub fn new() -> Self {
        Self
    }
}