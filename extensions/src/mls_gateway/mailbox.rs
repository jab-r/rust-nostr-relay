//! Mailbox services for key packages and welcome messages

/// Mailbox service functions
pub struct MailboxService;

impl MailboxService {
    /// Create a new mailbox service
    pub fn new() -> Self {
        Self
    }
}